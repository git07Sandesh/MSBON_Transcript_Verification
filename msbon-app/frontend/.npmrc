# React 19 broke a few peer-dep declarations on libraries that still
# advertise ^18 (notably react-pdf-highlighter-extended). The packages
# work fine with React 19 at runtime, but strict npm install fails. The
# Vercel build runs strict by default; this flag matches our local install.
legacy-peer-deps=true
